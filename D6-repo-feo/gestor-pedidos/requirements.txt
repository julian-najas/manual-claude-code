# Cierre completo de dependencias de ejecución, fijado el 27-ago-2026.
#
# Antes del módulo 09 este archivo tenía tres líneas sin versión: flask,
# requests y sqlalchemy. Esas tres líneas resuelven a QUINCE paquetes, así que
# había doce decisiones de versión que no tomaba nadie del equipo y que
# cambiaban solas entre una instalación y la siguiente.
#
# Se regenera, no se edita a mano:
#   pip install flask requests sqlalchemy && pip freeze > requirements.txt
#
# Directos: Flask, requests, SQLAlchemy. El resto entra por arrastre.
Flask==3.1.3
Jinja2==3.1.6
MarkupSafe==3.0.3
SQLAlchemy==2.0.52
Werkzeug==3.1.8
blinker==1.9.0
certifi==2026.7.22
charset-normalizer==3.5.1
click==8.5.0
greenlet==3.5.5
idna==3.19
itsdangerous==2.2.0
requests==2.34.2
typing_extensions==4.16.0
urllib3==2.7.0
